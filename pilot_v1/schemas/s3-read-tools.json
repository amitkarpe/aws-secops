[
  {
    "name": "check_s3_baseline",
    "description": "Read five deterministic controls for each bucket in the server-owned Pilot allowlist. The caller cannot select buckets and this tool never changes AWS.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "environment": {
          "type": "string",
          "description": "Use dev for the fixed Pilot v1 personal-lab resource."
        }
      },
      "required": ["environment"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "resource_id": {"type": "string"},
        "resource_name": {"type": "string"},
        "control": {"type": "string"},
        "status": {"type": "string"},
        "source": {"type": "string"},
        "recommendation": {"type": "string"},
        "buckets_checked": {"type": "integer"},
        "controls_checked": {"type": "integer"},
        "pass_count": {"type": "integer"},
        "fail_count": {"type": "integer"},
        "buckets": {"type": "array"},
        "exceptions": {"type": "array"},
        "mutation": {"type": "string"}
      },
      "required": ["resource_id", "resource_name", "control", "status", "source", "recommendation", "buckets_checked", "controls_checked", "pass_count", "fail_count", "buckets", "exceptions", "mutation"]
    }
  }
]
