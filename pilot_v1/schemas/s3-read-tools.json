[
  {
    "name": "check_s3_baseline",
    "description": "Read five deterministic controls on the fixed Pilot v1 S3 bucket in the dev lab. This tool never changes AWS.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "environment": {
          "type": "string",
          "description": "Use dev for the fixed Pilot v1 personal-lab resource."
        }
      },
      "required": ["environment"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "resource_id": {"type": "string"},
        "resource_name": {"type": "string"},
        "control": {"type": "string"},
        "status": {"type": "string"},
        "source": {"type": "string"},
        "recommendation": {"type": "string"},
        "controls": {"type": "array"},
        "mutation": {"type": "string"}
      },
      "required": ["resource_id", "resource_name", "control", "status", "source", "recommendation", "controls", "mutation"]
    }
  }
]
